name: my fist pipeline
on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: checkout code
              uses: actions/checkout@v4
            - name: build image
              run:  docker build -t esther/ciportofolio .
            - name: List images
              run:  docker images
